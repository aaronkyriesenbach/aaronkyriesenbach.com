+++
title = "Hello, world"
date = 2024-01-01
+++

This is a sample post. The `blog` section is marked `draft = true` in
`content/blog/_index.md`, so this page (and the whole section) is excluded
from `zola build` until that's removed.
